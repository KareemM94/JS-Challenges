function destroyer(arr, ...itemsToRemove) {
  console.log(itemsToRemove);
  let result = [];
  for(let i = 0; i < arr.length; i++){
    let item = arr[i];
    if(!itemsToRemove.includes(item)) {
      result.push(item);
    }
  }
  return result;
}

destroyer([1, 2, 3, 1, 2, 3], 2, 3);




